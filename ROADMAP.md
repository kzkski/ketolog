# Ketolog 実装ロードマップ

## 現行バージョン

`package.json` の `version` を正とする（本メモ記載時点では **1.23.1**）。以下はリリース履歴のメモ。

---

## v1.23.1 リリース済み（2026-04-12）

- コンビニ統合プリセットの表示名（JSON `name`）を **「コンビニ」** にした（ファイル名 `convenience-keto.json` は据え置き）

---

## v1.23.0 リリース済み（2026-04-12）

- コンビニ統合プリセットのファイル名を `convenience-keto.json` に変更（当初の店名表記は英語だったが、v1.23.1 で日本語化）

---

## v1.22.0 リリース済み（2026-04-12）

- コンビニ系プリセットを 1 ファイルに統合（チェーンは `group`。当初は `conbini-keto.json`／店名 `conbini`）

---

## v1.3.0 リリース済み（2026-04-10）

- ヘッダーのバージョンから変更履歴（`CHANGELOG.md`）への導線（環境変数 `NEXT_PUBLIC_CHANGELOG_URL`）
- Changelog 運用（`CONTRIBUTING.md`・PR テンプレ・CI ガードレール）

---

## v1.2.x リリース済み

- ドキュメント・開発フローの整理（CONTRIBUTING への集約、Cursor Project Rules、AGENTS 追記 等）
- **1.2.3**: 未使用の `api/presets` ルートを削除（プリセットは `public/presets` の静的配信 `/presets/` のみ）
- 以降の **1.2.x** は主にパッチ（バグ修正・小さなドキュメント更新）として積み上げる想定

---

## v1.1.0 リリース済み（2026-04-09）

- コンビニ向け JSON プリセットのファイル同梱（`convenience-keto.json`：アプリ上の店名は **コンビニ**、メニューは `group` でチェーン別。内容の完成度は品目により異なる）
- 新規ユーザー向け「汎用食材」プリセットの自動シード（`homemade-keto.json`。通常レストランとして投入）
- ヘッダーにバージョン番号表示
- バージョン管理方針策定（Semantic Versioning）

---

## v1.0.0 リリース済み（2026-04-09）

- 食事ログ記録・PFCバー・レストラン選択・カート・Supabase保存
- 認証（Supabase Auth。ログイン UI はメール／パスワードと Google OAuth を想定）
  - ※ 公開テスト環境では、主催者側の Supabase 設定により **メール／パスワードは無効**、**Google は特定メールドメインに制限**される場合があります（README の「一般向け非公開」と整合）。
- 日付ナビゲーション（過去ログ閲覧・編集・削除）
- メニュー管理（追加・編集・削除・ランク・100g換算）
- JSON エクスポート/インポート（テンプレート + プリセット）
- ユーザー設定（PFC目標値のカスタマイズ・全データエクスポート）

---

## Phase 1 完了

- 食事ログ記録（PFCバー・レストラン選択・カート・保存）
- 認証（上記 v1.0.0 のとおり）
- 初回シードデータ（空 → ユーザーが自分で登録）

---

## Phase 2 完了

### 2-1 完了
- メニューアイテムの追加／編集／削除
- レストランの追加／削除
- 栄養素入力の切替（100gあたり ↔ 1回分あたり）

### 2-2 進行中（Phase1実装）
- バーコード検索（Open Food Facts API）で市販品を取得
- `shared_products` キャッシュ基盤と `menu_items.shared_barcode` 参照を導入
- スキャン結果をメニュー追加に接続（OFF 未ヒット時は手入力で `shared_products` にも登録し他ユーザーと共有。RLS 本格は [#190](https://github.com/kzkski/ketolog/issues/190)）
- メニュー登録なしで食事ログへ記録（スナップショット）、食事タブ行右端の記録「＋」、カートの食事区分連動
- 「最近スキャン・検索した市販品だけを一覧する」**専用 UI／データ設計**は [#54](https://github.com/kzkski/ketolog/issues/54) で検討中。現状は OFF ルックアップと `shared_products`、メニュー行の `shared_barcode`、お気に入りで再選択を補助できるが、**時系列の「最近」一覧**そのものは未実装（仕様・優先度は Issue 側で未合意）。

### 2-3 後日
- 食材名検索（文部科学省食品成分表）

### 2-4 完了
- **レストラン設定のエクスポート／インポート（JSON）**
  - レストラン + メニューアイテムをまとめてJSONでエクスポート
  - JSONをアップロードしてインポート（既存と重複しない形で追加）
  - テンプレートダウンロード（AI へのプロンプトヒント付き）
  - **リポジトリ同梱のプリセット JSON** を `/presets/` で静的配信し、アプリからワンタップでインポート

### 2-5 完了
- **日付ナビゲーション・過去ログ管理**
  - 日付を前後に切り替えて過去の記録を閲覧
  - 過去の食事記録を編集・削除
  - 過去の日付に食事記録を追加

### 2-6 完了
- **ユーザー設定**
  - PFC目標値（P/F/C）のカスタマイズ
  - 全データをJSONでエクスポート

---

## Phase 3 後日（優先度低）

- AIによる食材名からPFC推定（BYOK: ユーザー自身のAnthropicキー）
- メニュー写真アップロード → Claude APIでメニュー自動生成
  - ※ 現状は外部チャットでメニューJSON生成 → 2-4のインポート機能で代替可能

---

## Phase 4 将来的な実装

- 体組成トラッキング（体重・体脂肪・筋肉量など）
- 睡眠・活動量の記録
- PFC推移グラフ・週次レポート
- フェーズ切替（README のとおり 導入期 / 脂肪燃焼期 / TKD の PFC プリセット）
- Ketovisor（AI総合ダイエット支援）との連携

---

## 公開・運用（ベータ／一般公開）

機能ロードマップとは別に、**リリースゲート・法務・インフラコスト・CI/QA・第三者コンプライアンス**のチェックリストは [docs/release/README.md](docs/release/README.md) を正とする。

## 市場調査（参考）

ベータ・一般公開に向けた外部調査の整理版は [docs/research/README.md](docs/research/README.md) を参照（上記リリースチェックリストとは別枠）。

---

## 方針メモ

- **Ketovisorとの分担**: 食事ログ・体組成の入力インターフェースに集中する。AIとのやり取りや分析はKetovisorで行う。KetovisorへのインプットとなるデータをKetologで整える。
- **AI機能の組み込み**: 優先度低。外部AIチャットで生成したデータをJSON経由でKetologに取り込む運用で当面対応。
- **市場・収益の前提**: 食事ログ単体でのマネタイズは成立しにくい市場になりやすいという認識を踏まえ、**構造化されたログデータの蓄積（基盤）**と、分析・対話・デバイス連携など**下流の価値（Ketovisor 等）**を一体で設計する。競合・数値の整理は [docs/research/README.md](docs/research/README.md) を参照。
- **地理的展開**: **初期は日本市場での検証を優先**し、海外展開は国際的なプライバシー規制、ヘルス連携（ネイティブアプリとの差）、既存のケト特化アプリとの競合を踏まえ**段階的に検討**する。
- **獲得経路の想定**: 汎用カロリーアプリ利用者への**完全乗り換えだけを前提にせず**、ケト管理用ツールとしての**併用導入から主利用への移行**を現実的な経路として想定する。
- **差別化の継続投資**: **コンビニ・外食のプリセット**、**JSON による共有・インポート**、ケト向け **PFC 記録体験**を、プロダクトの中核的な差別化として維持・磨き込む。
